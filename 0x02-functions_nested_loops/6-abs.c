#include "main.h"

/**
 * _abs - function that computes the absolute
 * value of an integer
 *
 * @n: tekes in integet type input for function
 *
 * Return: Alweays 0 (Success)
*/

int _ads(int n)
{
	if (n < 0)
		n= (-1) * n;
	return (n);
}
